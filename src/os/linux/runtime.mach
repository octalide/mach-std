use std.os.linux.constants;
use std.os.linux.syscall;

pub fun exit(code: i64) {
    masm {
        mov rax, SYS_EXIT
        mov rdi, code
        syscall
        hlt
    }
}

pub fun abort() {
    exit(255);
}
